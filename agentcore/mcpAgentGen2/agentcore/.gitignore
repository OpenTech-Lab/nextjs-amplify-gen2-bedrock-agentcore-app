# Secrets (local environment files are never committed)
.env.local

# CDK Build Artifacts
cdk/cdk.out/
cdk/node_modules/

# CLI Internals
.cli/*

# Ephemeral Staging
.cache/*

# deployed-state.json and aws-targets.json record this account's real AWS
# account ID, resource ARNs, and role names - kept local-only rather than
# committed to a public repo (see 2026-07 security review).
aws-targets.json
