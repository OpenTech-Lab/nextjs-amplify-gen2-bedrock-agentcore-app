NEXT_PUBLIC_AGENT_ARN=arn:aws:bedrock-agentcore:{region}:{accountId}:runtime/{runtimeName}
NEXT_PUBLIC_AWS_REGION=us-west-2

# S3 bucket backing the Knowledge Base document browser (server-only, no
# NEXT_PUBLIC_ prefix - never sent to the client bundle).
KNOWLEDGE_BASE_BUCKET={s3BucketName}